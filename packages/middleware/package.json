{
  "name": "@tempo-agentgate/middleware",
  "version": "0.1.0",
  "description": "Hono middleware that adds HTTP 402 paywall to API endpoints — verify on-chain TIP-20 payments on Tempo",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["agentgate", "hono", "middleware", "paywall", "402", "tempo"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/thescoho/agentgate"
  },
  "dependencies": {
    "@tempo-agentgate/core": "^0.1.0"
  },
  "peerDependencies": {
    "hono": ">=4.0.0",
    "viem": ">=2.0.0"
  }
}
