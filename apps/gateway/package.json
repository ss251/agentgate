{
  "name": "@tempo-agentgate/gateway",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@tempo-agentgate/core": "workspace:*",
    "@tempo-agentgate/middleware": "workspace:*"
  }
}
